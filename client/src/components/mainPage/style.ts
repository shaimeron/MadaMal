import { styleType } from "../../models";

export const style: styleType = {
    boxContainer: {
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: 'center'
    }
}